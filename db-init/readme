
## Záloha
docker exec -i pg_container_name /bin/bash -c "PGPASSWORD=pg_password pg_dump --username pg_username database_name" > /desired/path/on/your/machine/dump.sql
## aktualni
docker exec -i typeOrmSandBox /bin/bash -c "PGPASSWORD=postgres pg_dump --username postgres typeOrmSandBox" > o:\IdeaProjects\type-orm-sand-box\db-init\db.sql

## vypis containeru
docker container ls

